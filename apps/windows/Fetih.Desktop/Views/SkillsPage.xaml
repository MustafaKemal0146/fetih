<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Fetih.Desktop.Views.SkillsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:Fetih.Desktop.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24,16,24,16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock Text="Yetenekler" Style="{StaticResource PageTitleTextStyle}" />
            <TextBlock x:Name="SummaryText"
                       Style="{StaticResource MutedTextStyle}"
                       Text="Yetenek kataloğu taranıyor…" />
        </StackPanel>

        <!-- Arama + kategori süzgeci -->
        <Grid Grid.Row="1" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox x:Name="SearchBox"
                     Grid.Column="0"
                     AutomationProperties.AutomationId="skills_search"
                     PlaceholderText="Ara: ör. SQL injection, pcap, ATT&amp;CK, osint…"
                     TextChanged="SearchBox_TextChanged" />

            <ComboBox x:Name="CategoryBox"
                      Grid.Column="1"
                      MinWidth="220"
                      AutomationProperties.AutomationId="skills_category"
                      SelectionChanged="CategoryBox_SelectionChanged" />

            <Button x:Name="RefreshButton"
                    Grid.Column="2"
                    Content="Yenile"
                    AutomationProperties.AutomationId="skills_refresh"
                    Click="RefreshButton_Click" />
        </Grid>

        <Grid Grid.Row="2">
            <ProgressRing x:Name="LoadingRing"
                          Width="36"
                          Height="36"
                          IsActive="True"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center" />

            <TextBlock x:Name="EmptyText"
                       Visibility="Collapsed"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       TextAlignment="Center"
                       MaxWidth="480"
                       Style="{StaticResource MutedTextStyle}" />

            <!--
                ListView varsayılan olarak ItemsStackPanel ile sanallaştırma yapar;
                katalogda ~1000 SKILL.md olduğu için bu şart.
            -->
            <ListView x:Name="SkillList"
                      AutomationProperties.AutomationId="skills_list"
                      Visibility="Collapsed"
                      SelectionMode="Single"
                      ShowsScrollingPlaceholders="True"
                      ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Padding" Value="0,2" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:SkillInfo">
                        <Border Padding="12,10"
                                CornerRadius="6"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderThickness="1"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <StackPanel Spacing="2">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBlock Text="{x:Bind Name}"
                                               FontWeight="SemiBold"
                                               TextTrimming="CharacterEllipsis" />
                                    <TextBlock Text="{x:Bind SubtitleLabel}"
                                               FontSize="12"
                                               Opacity="0.55"
                                               VerticalAlignment="Center" />
                                </StackPanel>
                                <TextBlock Text="{x:Bind Description}"
                                           FontSize="12"
                                           Opacity="0.8"
                                           TextWrapping="Wrap"
                                           MaxLines="3"
                                           TextTrimming="CharacterEllipsis" />
                                <TextBlock Text="{x:Bind RelativePath}"
                                           FontSize="11"
                                           Opacity="0.45"
                                           FontFamily="Consolas"
                                           TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>

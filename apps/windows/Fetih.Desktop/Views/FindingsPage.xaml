<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Fetih.Desktop.Views.FindingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:Fetih.Desktop.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24,16,24,16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock Text="Bulgular" Style="{StaticResource PageTitleTextStyle}" />
            <TextBlock x:Name="SummaryText"
                       Style="{StaticResource MutedTextStyle}"
                       Text="Ajan tarafından üretilen güvenlik bulguları burada toplanır." />
        </StackPanel>

        <!-- Ciddiyet süzgeci — koleksiyon dolduğunda anlam kazanır -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8">
            <ComboBox x:Name="SeverityBox"
                      MinWidth="200"
                      AutomationProperties.AutomationId="findings_severity"
                      SelectionChanged="SeverityBox_SelectionChanged" />
            <TextBlock x:Name="CountText"
                       VerticalAlignment="Center"
                       Style="{StaticResource MutedTextStyle}" />
        </StackPanel>

        <Grid Grid.Row="2">
            <!-- Boş durum -->
            <Border x:Name="EmptyState"
                    Style="{StaticResource CardBorderStyle}"
                    MaxWidth="620"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                <StackPanel Spacing="10" HorizontalAlignment="Center">
                    <FontIcon Glyph="&#xE7BA;"
                              FontSize="28"
                              HorizontalAlignment="Center"
                              Opacity="0.5" />
                    <TextBlock Text="Henüz bulgu yok"
                               FontWeight="SemiBold"
                               FontSize="16"
                               HorizontalAlignment="Center" />
                    <TextBlock TextAlignment="Center"
                               Style="{StaticResource MutedTextStyle}"
                               Text="Bir tarama veya görev tamamlandığında bulgular burada listelenecek. Faz 1'de Masaüstü Köprüsü henüz bağlı olmadığı için ajandan veri akmıyor; sayfa yapısı hazır ve köprü bağlandığında doldurulacak." />
                    <TextBlock TextAlignment="Center"
                               FontSize="12"
                               Opacity="0.55"
                               TextWrapping="Wrap"
                               Text="Yalnızca yetkili olduğun sistemlerde test yap." />
                </StackPanel>
            </Border>

            <ListView x:Name="FindingList"
                      AutomationProperties.AutomationId="findings_list"
                      Visibility="Collapsed"
                      SelectionMode="Single"
                      ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Padding" Value="0,3" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:Finding">
                        <Border Padding="14,12"
                                CornerRadius="8"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderThickness="1"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <StackPanel Spacing="6">
                                <StackPanel Orientation="Horizontal" Spacing="10">
                                    <Border Padding="8,2"
                                            CornerRadius="4"
                                            VerticalAlignment="Center"
                                            Background="{x:Bind SeverityBrushKey, Converter={StaticResource ThemeBrushConverter}}">
                                        <TextBlock Text="{x:Bind SeverityLabel}"
                                                   FontSize="11"
                                                   FontWeight="SemiBold" />
                                    </Border>
                                    <TextBlock Text="{x:Bind Title}"
                                               FontWeight="SemiBold"
                                               VerticalAlignment="Center"
                                               TextTrimming="CharacterEllipsis" />
                                    <TextBlock Text="{x:Bind TimeLabel}"
                                               FontSize="11"
                                               Opacity="0.5"
                                               VerticalAlignment="Center" />
                                </StackPanel>

                                <TextBlock Text="{x:Bind Target}"
                                           FontFamily="Consolas"
                                           FontSize="12"
                                           Opacity="0.75"
                                           TextTrimming="CharacterEllipsis" />

                                <TextBlock Text="{x:Bind Evidence}"
                                           FontSize="12"
                                           TextWrapping="Wrap"
                                           MaxLines="4"
                                           Opacity="0.85" />

                                <TextBlock Text="{x:Bind Recommendation}"
                                           FontSize="12"
                                           TextWrapping="Wrap"
                                           Opacity="0.7" />

                                <TextBlock Text="{x:Bind Reference}"
                                           FontSize="11"
                                           Opacity="0.6"
                                           Visibility="{x:Bind HasReference, Converter={StaticResource BoolToVisibilityConverter}}" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>
